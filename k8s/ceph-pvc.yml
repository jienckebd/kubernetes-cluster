---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cluster-cache-composer
  namespace: ops
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: gp2
