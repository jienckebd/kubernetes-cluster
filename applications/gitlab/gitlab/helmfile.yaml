helmDefaults:
  disableValidation: true

repositories:
  - name: gitlab
    url: https://charts.gitlab.io

releases:
  - name: gitlab
    namespace: gitlab
    chart: gitlab/gitlab
    version: 6.7.0
    installed: true
    values:
      - values.yaml

environments:
  gitlab: {  }
