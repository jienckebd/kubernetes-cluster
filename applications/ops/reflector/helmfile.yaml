repositories:
  - name: emberstack
    url: https://emberstack.github.io/helm-charts

releases:
  - name: reflector
    namespace: ops
    chart: emberstack/reflector
    version: 6.1.47
    installed: true

    values:
      - values.yaml

environments:
  ops: {  }
