repositories:
  - name: rook
    url: https://charts.rook.io/release

releases:
  - name: rook-ceph
    namespace: rook-ceph
    chart: rook/rook-ceph
    version: 1.8.4
    installed: true
    values:
      - values.yaml

environments:
  rook-ceph: {  }
