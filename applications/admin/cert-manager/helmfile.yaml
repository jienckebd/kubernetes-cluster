repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: cert-manager
    namespace: env-{{ .Environment.Name }}
    chart: bitnami/cert-manager
    installed: true

    values:
      - values.yaml

environments:
  admin: {  }
