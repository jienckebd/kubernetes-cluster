repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: keycloak
    namespace: admin
    chart: bitnami/keycloak
    version: 13.0.0
    installed: true
    values:
      - values.yaml

environments:
  admin: {  }
