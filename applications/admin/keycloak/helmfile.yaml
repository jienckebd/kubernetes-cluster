repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: keycloak
    namespace: env-{{ .Environment.Name }}
    chart: bitnami/keycloak
    installed: true

    values:
      - values.yaml

environments:
  admin: {  }
