repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: keycloak
    namespace: admin
    chart: bitnami/keycloak
    installed: true

    values:
      - values.yaml

environments:
  admin: {  }
