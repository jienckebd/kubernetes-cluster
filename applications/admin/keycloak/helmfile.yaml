repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: keycloak
    namespace: admin
    chart: bitnami/keycloak
    # version: 10.1.0
    installed: true
    values:
      - values.yaml

environments:
  admin: {  }
