ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: 'false'
  hosts:
    - k8s.sys.sysf.one
  tls:
    - secretName: tls-sys
      hosts:
      - k8s.sys.sysf.one
