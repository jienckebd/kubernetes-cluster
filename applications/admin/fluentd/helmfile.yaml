repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: fluentd
    namespace: admin
    chart: bitnami/fluentd
    installed: true

    values:
      - values.yaml

environments:
  admin: {  }
