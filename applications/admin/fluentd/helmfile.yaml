repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: fluentd
    namespace: admin
    chart: bitnami/fluentd
    version: 5.3.2
    installed: true

    values:
      - values.yaml

environments:
  admin: {  }
