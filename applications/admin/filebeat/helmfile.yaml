repositories:
  - name: elastic
    url: https://helm.elastic.co

releases:
  - name: filebeat
    namespace: admin
    chart: elastic/filebeat
    installed: true
    values:
      - values.yaml

environments:
  admin: {  }
