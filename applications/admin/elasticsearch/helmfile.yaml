repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: elasticsearch
    namespace: admin
    chart: bitnami/elasticsearch
    # version: 17.9.3
    installed: true

    values:
      - values.yaml

environments:
  admin: {  }
