data:
  # autoscaling:
  #   enabled: true
  #   minReplicas: 2
  #   maxReplicas: 5
  #   targetCPU: 90
  resources:
    limits: {}
    requests:
      cpu: 100m
      memory: 512Mi
  persistence:
    storageClass: efs-sc
    accessModes:
      - ReadWriteMany

master:
  # autoscaling:
  #   enabled: true
  #   minReplicas: 2
  #   maxReplicas: 5
  #   targetCPU: 90
  resources:
    limits: {}
    requests:
      cpu: 100m
      memory: 512Mi
  persistence:
    storageClass: efs-sc
    accessModes:
      - ReadWriteMany

coordinating:
  # autoscaling:
  #   enabled: true
  #   minReplicas: 2
  #   maxReplicas: 5
  #   targetCPU: 90
  resources:
    limits: {}
    requests:
      cpu: 100m
      memory: 512Mi

ingest:
  # autoscaling:
  #   enabled: true
  #   minReplicas: 2
  #   maxReplicas: 5
  #   targetCPU: 90
  resources:
    limits: {}
    requests:
      cpu: 100m
      memory: 512Mi

metrics:
  enabled: true
  serviceMonitor:
    enabled: true
