repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: grafana
    namespace: admin
    chart: bitnami/grafana
    version: 7.6.14
    installed: true

    values:
      - values.yaml

environments:
  admin: {  }
